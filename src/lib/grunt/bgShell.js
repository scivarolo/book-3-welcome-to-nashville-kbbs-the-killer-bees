module.exports = {
            launchAPI: {
                cmd: "json-server -p 8088 -w ../../api/database.json",
                cmd: "json-server -p 8088 -w ../../api/eventbriteCategories.json"
            },
            _defaults: {
                bg: true
            }
        }
