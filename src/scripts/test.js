// theEvent.venue.address.address_1
// theEvent.venue.address.city
// theEvent.venue.address.localized_multi_line_address_display.toJoin(",")
// let eventVenue = events.venue.name
// function vetting name (eventVenue){
    // if(eventVenue === " " || eventVenue === null){ eventVenue = " " DO NOT INSERT INTO CODE } else
// }
// theEvent.venue.name